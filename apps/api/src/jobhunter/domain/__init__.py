"""Domain layer (ADR-003): pure business models and rules.

**Zero framework imports** — no FastAPI, SQLAlchemy, Redis, etc. Everything here is
unit-testable without a database or HTTP server. Populated as features land.
"""
